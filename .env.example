DEBUG=true
SECRET_KEY=change-me
ALLOWED_HOSTS=127.0.0.1,localhost

# Database
# Dev uses SQLite by default. For Postgres (prod), set DATABASE_URL:
# DATABASE_URL=postgres://USER:PASSWORD@HOST:5432/DBNAME

# Email (dev prints to console by default)
DEFAULT_FROM_EMAIL=booknest@example.com

